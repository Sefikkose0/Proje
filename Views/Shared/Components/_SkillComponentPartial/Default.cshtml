@model List<webprojesi.DAL.Entities.Yetenek>
<div class="col-six tab-full right">
    <h3>Programa Dilleri & Teknolojiler</h3>

    <ul class="skill-bars">
        @foreach (var item in Model)
        {
            <li>
                <div class="progress percent90"><span>@item.Deger%</span></div>
                <strong>@item.Baslik</strong>
            </li>

        }

    </ul>
</div>